void Drawcontour( int x, int y, int w, int h) {

  fill(205);
  stroke(30);
  rect(x, y, w, h, 10);
}

void Drawcontourx( int x, int y, int w, int h) {

  fill(130);
  stroke(230);
  rect(x, y, w, h, 10);
}
